[pytest]
filterwarnings =
    ignore::PytzUsageWarning
markers =
    login_guest: tests for registered users
    slow: marks tests as slow (deselect with '-m "not slow"')
    serial
    need_review: important tests for review
    test_user: test for users
